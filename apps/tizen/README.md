# Samsung Tizen TV

This shell packages the shared Alfie TV web application for Samsung Tizen TVs. Build the shared web app first, then use `scripts/package-smart-tvs.mjs` to create the unsigned `.wgt` development artifact.

Production installation/distribution requires Samsung signing with a valid certificate profile.
