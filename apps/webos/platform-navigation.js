(() => { const keyMap = {37:'ArrowLeft',38:'ArrowUp',39:'ArrowRight',40:'ArrowDown',13:'Enter',461:'Escape',415:'MediaPlayPause',19:'MediaPlayPause'}; document.addEventListener('keydown', event => { const key=keyMap[event.keyCode]; if (!key || event.key===key) return; try { event.preventDefault(); } catch {} window.dispatchEvent(new KeyboardEvent('keydown',{key,code:key,bubbles:true})); }, true); })();
